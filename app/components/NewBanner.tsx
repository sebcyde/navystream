'use client';
import {
	GetAnimeEpisodes,
	GetAnime,
	GetAnimeVideos,
	GetAnimeRelations,
} from '@/functions/Anime';

export const NewBanner = async () => {
	// await GetAnime();
	// await GetAnimeEpisodes(1);
	// await GetAnimeVideos(1);
	// await GetAnimeRelations(1);

	return (
		<div>
			<h3>New Banner</h3>
		</div>
	);
};
